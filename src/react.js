/**
 * Pre-configured React.js app.
 * Don't change anything in this file unless you know what you're doing
 * The root react app element is include inside the /src/react-app folder.
 * We suggest you build your react components here and render them in the App component
 */
import React from 'react'
import ReactDOM from 'react-dom'
import App from './react-app/App'

ReactDOM.render(<App />, document.getElementById('container'))
